```
#include <stdint.h>
#include <stdbool.h>
#include <myknowledge.h>

int main(void) {
  enum prons_t pronounce = HE_HIM;
  enum os_t os = ARCH_LINUX;
  enum favlang_t main_lang = C;
  enum country_t country = UKRAINE;

  size_t years_old = 15;
  bool furry = true;

  return 0;
}
```
